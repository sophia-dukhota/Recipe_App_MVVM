<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="_6002CEM_SophiaDukhota.Views.MainAppPage"
             xmlns:model="clr-namespace:_6002CEM_SophiaDukhota.Models"
             xmlns:local="clr-namespace:_6002CEM_SophiaDukhota.ViewModels"
             x:DataType="local:MainAppPageViewModel"
             Title="Main Page">
<!--
    <ContentPage.BindingContext>
        <local:MainAppPageViewModel/>
    </ContentPage.BindingContext>
    -->


    <!--
        <Label 
            Text="Welcome to .NET MAUI!"
            VerticalOptions="Center" 
            HorizontalOptions="Center" />
        <Button x:Name="getData"
                Text="GetData"
                Command="{Binding GetRecipesCommand}"/> -->
    <Grid
        ColumnDefinitions="*, *"
        ColumnSpacing="5"
        RowDefinitions="*, Auto"
        RowSpacing="0">

        <Button Grid.Column="0"
                Grid.Row="1"
        x:Name="getData"
        Text="GetData"
        IsEnabled="{Binding IsNotBusy}"
        Command="{Binding GetRecipesCommand}"/>

            <CollectionView ItemsSource="{Binding recipes}"
                            Grid.ColumnSpan="2">
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="model:Recipe">
                        <Grid Padding="10">
                            <Frame>
                                <Grid Padding="0" ColumnDefinitions="125, *">
                                    <Image Aspect="AspectFill" Source="{Binding image}"/>
                                    <StackLayout Grid.Column="1"
                                                 Grid.RowSpan="2"
                                                 Padding="10"
                                                 VerticalOptions="Center">
                                        <Label Text="{Binding label}"/>
                                    </StackLayout>
                                </Grid>
                            </Frame>
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <!--
            <CollectionView ItemsSource="{Binding recipes}"
                            Grid.ColumnSpan="2">
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="model:Recipe">
                        <Grid Padding="10">
                            <Frame HeightRequest="125">
                                <Grid Padding="0" ColumnDefinitions="125, *">
                                    <Image Aspect="AspectFill" Source="{Binding image}"
                                           WidthRequest="125"
                                           HeightRequest="125"/>
                                    <StackLayout Grid.Column="1"
                                                 Grid.RowSpan="2"
                                                 Padding="10"
                                                 VerticalOptions="Center">
                                        <Label Text="{Binding label}"/>
                                    </StackLayout>
                                </Grid>
                            </Frame>
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
            -->
    </Grid>
        

<!--
        <ListView ItemsSource="{Binding recipesSearchModel}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Text="{Binding recipesSearchModel.q}"/>

                </DataTemplate>

            </ListView.ItemTemplate>

        </ListView> 
     
     -->

        
       <!-- <Label Text="{Binding RecipesSearchModel, Mode=TwoWay}"/>
        <Label Text="{Binding RecipesSearchModel.hits[0].recipe.label}"/>
        <Label Text="{Binding Label}"/> -->

</ContentPage>